{
  "name": "pwa-pint",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd src && bower install && cd .. && npm install",
    "build": "node node_modules/pwa-build build --config config/main.js --fb",
    "build:dev": "npm run build",
    "build:prod": "node node_modules/pwa-build build --config config/main.js --min --es5 --sw --sitemap --fb",
    "dev": "npm run start && npm run build:dev && node node_modules/pwa-build dev --config config/main.js",
    "deploy": "npm run build:prod && firebase deploy --only functions,hosting",
    "📦": "npm run start",
    "🔥": "npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "pwa-build": "git+ssh://git@gitlab.pint.com/pwa/pwa-build.git#0.0.4"
  },
  "dependencies": {}
}
